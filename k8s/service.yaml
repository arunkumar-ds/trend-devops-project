apiVersion: v1
kind: Service
metadata:
  name: trend-service
spec:
  type: LoadBalancer
  selector:
    app: trend
  ports:
    - protocol: TCP
      port: 80         # The port you type in your browser
      targetPort: 3000 # The port inside your Docker container
